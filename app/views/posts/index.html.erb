<div class="col-10 offset-1 col-sm-8 offset-sm-2 col-md-6 offset-md-3">
  <% @posts.each do |post| %>
    <%= link_to post_path(post) do %>
      <%= image_tag post.photo, class: 'thumbnail' %>
    <% end %>
  <% end %>
</div>
